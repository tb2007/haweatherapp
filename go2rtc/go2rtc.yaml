api:
  listen: ":1984"

ffmpeg:
  bin: ffmpeg

streams:
  camera:
    - ${RTSP_URL}
    - ffmpeg:camera#video=mjpeg
  camera2:
    - ${RTSP_URL_2}
    - ffmpeg:camera2#video=mjpeg

log:
  level: warn
